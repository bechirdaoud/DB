# lib structure

- `api/`: data access clients and API wrappers.
- `auth/`: authentication/session helpers.
- `constants/`: static app constants (routes, labels, etc.).
- `types/`: shared TypeScript domain types.
- `utils.ts`: shared utility helpers.
